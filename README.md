# Semantic Components Analysis (SCA)

Perform linear dimensionality reduction on word-pair vectors from a word embeddings model in order to visualize semantic relationships captured by the embedding.
